# Third-Party Notice

Cryptographic tasks use OpenSSL as a verification dependency. OpenSSL is not
vendored in this repository and is tracked as an Apache-2.0 runtime/library
dependency in the repository-level notice.

NIST standard PDFs are not bundled. Each task's `references/README.md` points
to the official NIST DOI page.
