# Third-Party Notice

SingleCellAnalysis tasks reference OpenProblems Bio task repositories and
public datasets:

- `task_denoising`, `task_perturbation_prediction`, and `task_predict_modality`:
  MIT.
- OpenProblems/Kaggle/S3 datasets are downloaded externally and are not
  redistributed here.
- `denoising_ttt` also references `test-time-training/discover`, `simscity`,
  and `molecular-cross-validation`, all tracked as MIT dependencies/context.

Standard license texts are in `../../LICENSES/third_party/`; repository-level
provenance is in `../../THIRD_PARTY_NOTICES.md`.
