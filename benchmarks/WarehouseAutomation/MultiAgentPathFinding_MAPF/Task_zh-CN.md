# 仓库多机器人路径规划

## 背景

该任务刻画一个典型的仓储机器人协同问题：多台机器人需要在仓库栅格中移动到目标位置，同时避免碰撞和对向换位冲突。

## 问题定义

给定带障碍物的仓库地图和一组固定机器人，输出每个机器人的一条离散时间路径。路径由随时间变化的栅格坐标序列组成。

## 有效性约束

只有满足以下条件时解才有效：

1. 每条路径从指定起点开始。
2. 每条路径在指定终点结束。
3. 每一步只能原地等待或移动一个曼哈顿单位。
4. 机器人不能出界，不能进入障碍物。
5. 任意时刻不能有两个机器人占据同一格。
6. 任意相邻时刻不能出现两个机器人对向换位。

## 输出格式

写出 `paths.json`：

```json
{
  "instance_id": "warehouse_small_v1",
  "method": "...",
  "paths": {
    "agent_0": [[0, 0], [0, 1]],
    "agent_1": [[6, 0], [6, 1]]
  }
}
```

## 评分方式

对于有效解：

- `makespan` = 最长路径长度减一
- `sum_of_costs` = 所有机器人移动总步数
- `combined_score = 0.6 / (1 + makespan) + 0.4 / (1 + sum_of_costs)`

非法输出记为：

- `valid = 0`
- `combined_score = 0`

<!-- AI_GENERATED -->
