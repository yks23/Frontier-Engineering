# PSPLIB j3010_1 RCPSP

在工序先后关系和可再生资源约束下调度全部任务。

## 评分方式

- `candidate_makespan` = 汇点任务完成时间
- `best_known_makespan = 42`
- `combined_score = 42 / candidate_makespan`
- `human_best_score = 1.0`

非法输出记为 `combined_score = 0` 且 `valid = 0`。

<!-- AI_GENERATED -->
